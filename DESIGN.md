# Quadfall 設計書

## 1. 概要

| 項目 | 内容 |
|------|------|
| ゲーム名 | Quadfall |
| ジャンル | 落ち物パズル（テトリスライク） |
| ターゲット機 | Arduboy（Arduino Leonardo互換） |
| ライブラリ | Arduboy2 |

---

## 2. ハードウェア仕様

詳細は `BOARD.md` を参照。

---

## 3. 画面レイアウト

### タイトル画面

| 要素 | X座標 | Y座標 | 備考 |
|------|-------|-------|------|
| "QUADFALL"（2倍サイズ） | 20 | 18 | setTextSize(2) |
| "PRESS A TO START" | 22 | 42 | — |
| "[B] SOUND: ON/OFF" | 28 | 54 | 音声状態により ON または OFF を表示 |

### ゲーム画面

```
+--+----------+--+----------------+
|  | フィールド |  |  サイドパネル  |
|  | 10×16    |  |                |
|  | (4px/cel)|  | SCORE:         |
|  |          |  | 000000         |
|  |          |  |                |
|  |          |  | LEVEL:         |
|  |          |  | 01             |
|  |          |  |                |
|  |          |  | NEXT:          |
|  |          |  | [ネクスト図]   |
|  |          |  |                |
+--+----------+--+----------------+
 1px border  1px  残り幅: 82px
 フィールド幅: 40px (10列 × 4px)
 フィールド高さ: 64px (16行 × 4px)
```

#### ピクセル割り当て

| 要素 | X座標 | Y座標 | 幅×高さ |
|------|-------|-------|---------|
| フィールド枠 | 0 | 0 | 42×64 |
| フィールド内部 | 1 | 0 | 40×64 |
| スコアラベル | 46 | 0 | — |
| スコア値 | 46 | 9 | — |
| レベルラベル | 46 | 24 | — |
| レベル値 | 46 | 33 | — |
| ネクストラベル | 46 | 48 | — |
| ネクスト図 | 46 | 56 | 20×8 |

### ゲームオーバー画面

- フィールドとサイドパネルを背景としてそのまま表示
- オーバーレイ矩形（黒塗り→白枠）: X=4, Y=22, 幅36×高さ20 px
- "GAME": X=7, Y=26
- "OVER": X=7, Y=34

### ポーズ画面

- 画面全体を黒塗りにし、フィールドは非表示（覗き見防止）
- 中央に "PAUSED" テキストと再開方法を表示

| 要素 | X座標 | Y座標 | 備考 |
|------|-------|-------|------|
| "PAUSED"（2倍サイズ） | 28 | 22 | setTextSize(2) |
| "A+B TO RESUME" | 16 | 46 | — |

---

## 4. ゲームピース（テトロミノ）

7種類のピース（各4ブロック）。回転状態は4パターン（0°/90°/180°/270°）。

| 名前 | 形 | ID |
|------|----|----|
| I | □□□□ | 0 |
| O | □□ / □□ | 1 |
| T | □□□ / 中下 | 2 |
| S | _□□ / □□_ | 3 |
| Z | □□_ / _□□ | 4 |
| J | □__ / □□□ | 5 |
| L | __□ / □□□ | 6 |

各ピースの形状は4×4グリッドの16ビットマスクで表現し、PROGMEM（Flash）に格納する。

---

## 5. データ構造

### フィールド

```cpp
// 10列 × 16行 = 160ビット → uint16_t[16] (各行を10ビット使用)
uint16_t field[16];
```

### 現在ピース

```cpp
struct Piece {
    int8_t x;       // フィールド左端からの列位置
    int8_t y;       // フィールド上端からの行位置
    uint8_t type;   // ピース種類 (0〜6)
    uint8_t rot;    // 回転状態 (0〜3)
};
```

### ゲーム状態

```cpp
enum GameState {
    STATE_TITLE,
    STATE_PLAYING,
    STATE_PAUSED,
    STATE_GAMEOVER
};
```

---

## 6. 操作系

| ボタン | 動作 |
|--------|------|
| LEFT | ピースを左に移動 |
| RIGHT | ピースを右に移動 |
| DOWN | ソフトドロップ（1段落下、スコア加算） |
| A | ピースを反時計回りに回転 |
| B | ピースを時計回りに回転 |
| UP | ハードドロップ（即座に落下、スコア加算） |
| A+B 同時押し（ゲーム中） | ポーズ |
| A+B 同時押し（ポーズ中） | ゲーム再開 |
| A（タイトル） | ゲーム開始 |
| A（ゲームオーバー） | タイトルに戻る |

### 入力の連射制御

長押し時に一定フレーム後から連射が有効になる（DAS: Delayed Auto Shift）。

| パラメータ | 値 |
|-----------|-----|
| 初回遅延 | 16フレーム |
| 連射間隔 | 6フレーム |

---

## 7. ゲームロジック

### 落下タイミング（レベル別）

| レベル | 落下間隔（フレーム） |
|--------|-------------------|
| 1 | 48 |
| 2 | 43 |
| 3 | 38 |
| 4 | 33 |
| 5 | 28 |
| 6 | 23 |
| 7 | 18 |
| 8 | 13 |
| 9 | 8 |
| 10 | 6 |

フレームレート: 60fps

### ライン消去とレベルアップ

- 一度に消去したライン数に応じてスコア加算
- 合計消去ライン数が `level × 10` を超えるとレベルアップ

### スコア計算

| 消去ライン数 | 加算スコア（× レベル） |
|------------|----------------------|
| 1 | 100 |
| 2 | 300 |
| 3 | 500 |
| 4 | 800 |
| ソフトドロップ 1段 | 1 |
| ハードドロップ 1段 | 2 |

### ゲームオーバー条件

新しいピースが出現した時点でフィールドと重なる場合。

---

## 8. SRS（スーパーローテーションシステム）簡易版

回転時に壁や他ブロックと重なる場合、以下のオフセットでキックを試みる。

| 試行順 | オフセット |
|--------|-----------|
| 1 | (0, 0)（そのまま） |
| 2 | (-1, 0)（左に1） |
| 3 | (+1, 0)（右に1） |
| 4 | (0, -1)（上に1） |

---

## 9. ゴースト（着地予測）

現在のピースと同じ形・回転状態で、重力方向にそのまま落下させたときの最終着地位置を半透明風に表示する機能。

- `ghostY()` で計算: ピースを Y+1 方向に 1 行ずつ移動し、衝突する直前の Y 座標を返す
- 現在ピースとゴーストが同じ Y 座標の場合（すでに接地中）は描画しない
- 描画: `drawRect`（塗り潰しなし、枠のみ）で点線風の外観

---

## 10. テトロミノのランダム生成

線形合同法（LCG）による簡易乱数生成を使用する。RAM に乱数状態変数（uint16_t）1つを保持するのみで、stdlib の `rand()` より軽量。

| 項目 | 内容 |
|------|------|
| アルゴリズム | `rng = rng * 25173 + 13849`（uint16_t） |
| シード初期化 | `gameStart()` 呼び出し時に `millis() ^ 0xA5A5` を設定 |
| ピース選択 | 生成値を 7 で割った余り（0〜6）をピース種別に使用 |

---

## 11. 効果音

### ライブラリ

`ArduboyTones`（Arduboy2に同梱）を使用する。音データは周波数＋継続時間のペア列をPROGMEMに格納する。同時発音は1音のみ。

### 効果音一覧

| イベント | 音のイメージ | 音数 |
|----------|-------------|------|
| ピースロック | 低い短音（タン） | 1音 |
| ライン消去（1〜3ライン） | 上昇音（ピロリ） | 2音 |
| テトリス（4ライン） | 上昇音×2（ピロリリ） | 4音 |
| ハードドロップ | 低音（ドスン） | 1音 |
| レベルアップ | 3音ファンファーレ | 3音 |
| ゲームオーバー | 下降音（ズーン） | 3音 |

### 音のオン/オフ

| 項目 | 内容 |
|------|------|
| 切り替えボタン | タイトル画面でBボタン |
| 表示 | タイトル画面に `[B] SOUND: ON / OFF` を表示 |
| 保存 | EEPROMに保存（電源を切っても設定が残る） |
| デフォルト | ON |

### ファイル

| ファイル | 役割 |
|----------|------|
| `sound.h` | プロトタイプ宣言・EEPROM定数 |
| `sound.cpp` | 音データ（PROGMEM）・再生関数・設定の読み書き |

---

## 12. ファイル構成

```
Quadfall/
├── include/
│   ├── game.h            # 定数・構造体・プロトタイプ宣言
│   ├── renderer.h        # 描画プロトタイプ宣言
│   ├── input.h           # 入力プロトタイプ宣言
│   └── sound.h           # 効果音プロトタイプ宣言
├── src/
│   ├── Quadfall.cpp      # メインスケッチ（setup/loop）
│   ├── game.cpp          # ゲームロジック（ピース・フィールド・スコア）
│   ├── renderer.cpp      # 描画処理
│   ├── input.cpp         # ボタン入力・DAS処理
│   └── sound.cpp         # 効果音データ・再生・設定保存
├── platformio.ini        # PlatformIO ビルド設定
├── BOARD.md              # ハードウェア仕様
├── CLAUDE.md             # Claude Code 向けガイダンス
├── DESIGN.md             # 本設計書
└── README.md             # プロジェクト概要・操作方法
```

---

## 13. メインループの流れ

```
loop()
 ├─ arduboy.nextFrame() で60fps制御
 ├─ input: ボタン状態更新
 ├─ update:
 │   ├─ STATE_TITLE    → Aボタンでゲーム開始
 │   ├─ STATE_PLAYING  → 移動・回転・落下・ライン消去・ゲームオーバー判定
 │   └─ STATE_GAMEOVER → Aボタンでタイトルへ
 └─ render:
     ├─ フィールド描画
     ├─ 現在ピース描画
     ├─ ゴースト（着地予測）描画
     └─ スコア・レベル・ネクスト描画
```

---

## 14. メモリ見積もり

| 項目 | サイズ |
|------|--------|
| フィールド配列 | 32 B |
| ピースデータ (PROGMEM) | 約 128 B |
| 効果音データ (PROGMEM) | 約 80 B |
| スタック・変数 | 約 50 B |
| Arduboy2ライブラリ | 約 1KB |
| **合計 (RAM)** | **< 2KB（余裕あり）** |
