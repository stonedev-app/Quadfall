# Quadfall コード理解ガイド

コードを段階的に読み解くための自己学習ガイド。  
各ステップを完了したら `[ ]` を `[x]` に変え、ヘッダーの進捗数を更新する。

## 進捗：0 / 7 ステップ完了

---

## コードの全体像

### ファイル構成

| ファイル | 行数 | 役割 |
|---|---|---|
| `src/Quadfall.cpp` | 29行 | `setup()` / `loop()` だけのエントリポイント |
| `include/game.h` + `src/game.cpp` | 70 + 268行 | ゲームロジック全体（ピース・フィールド・スコア） |
| `src/renderer.cpp` | 201行 | 画面描画（4つの画面状態ごとに描き分け） |
| `src/input.cpp` | 109行 | ボタン入力・DAS（連射）制御 |
| `src/sound.cpp` | 102行 | 効果音データ（PROGMEM）と再生 |

### 1フレームの流れ

```
loop()
  ├─ arduboy.nextFrame()    // 60fps 待機
  ├─ arduboy.pollButtons()  // ボタン状態スキャン
  ├─ inputUpdate()          // 入力 → ピース移動・回転
  ├─ gameUpdate()           // 重力落下・ライン消去
  └─ renderFrame()          // 画面描画 → display()
```

### ゲーム状態の遷移

```
STATE_TITLE ──(Aボタン)──► STATE_PLAYING
                               │
                           (A+B同時)
                               │
                          STATE_PAUSED ──(A+B同時)──► STATE_PLAYING
                               │
                         (出現時重なり)
                               │
                          STATE_GAMEOVER ──(Aボタン)──► STATE_TITLE
```

---

## Step 1：仕様を読む [ ]

**目安：30分**

| 読むファイル | 目的 |
|---|---|
| `README.md` | 操作方法・ゲームルール |
| `BOARD.md` | ハードウェア仕様（128×64 OLED、2.5KB RAM） |
| `DESIGN.md` | 仕様・画面レイアウト・スコア計算・メモリ見積もり |

### 確認ポイント

- [ ] ライン消去のスコア計算式（1行・2行・3行・4行で何点か）を説明できる
- [ ] レベルアップする条件を説明できる
- [ ] ハードドロップとソフトドロップの違いを説明できる

---

## Step 2：メインループとデータ構造を読む [ ]

**目安：30〜45分**

| 読むファイル | 目的 |
|---|---|
| `src/Quadfall.cpp` | ゲームループの最上位構造（全29行） |
| `include/game.h` | 定数・構造体・グローバル変数の宣言 |

### 重点理解事項

**`Piece` 構造体（`game.h`）**：
```cpp
struct Piece {
    int8_t  x;        // フィールド左端からの列位置
    int8_t  y;        // フィールド上端からの行位置
    uint8_t type;     // ピース種類（0〜6: I,O,T,S,Z,J,L）
    uint8_t rot;      // 回転状態（0〜3: 0°/90°/180°/270°）
};
```

**フィールド配列**：`uint16_t field[16]` → 1行を1つの整数（16ビット）で表現  
→ `field[row] & (1u << (FIELD_W - 1 - col))` でその列にブロックがあるか判定

### 実験

- [ ] `col=0` のとき何ビット目か手計算で確認する（答え：ビット9）
- [ ] `col=9` のとき何ビット目か手計算で確認する（答え：ビット0）

---

## Step 3：ゲームロジックの核心を読む [ ]

**目安：1〜1.5時間**

読むファイル：`src/game.cpp`

### 関数ごとの理解ポイント

| 関数 | 理解するポイント |
|---|---|
| `PIECES[7][4]`（PROGMEM定数） | 4×4 グリッドを16ビットマスクで表現する仕組み |
| `pieceCanPlace()` | `pgm_read_word()` でFlashからピース形状を読み出す理由 |
| `pieceMove()` | 衝突判定→移動実行の流れ |
| `pieceRotate()` | SRS壁キックの簡易実装（オフセット試行の順序） |
| `ghostY()` | 着地予測Y座標の計算方法 |
| `pieceLock()` | ピースをフィールドに「書き込む」ビット演算 |
| `clearLines()` | 満杯行の検出・削除・スコア計算・レベルアップの連鎖 |
| `spawnNext()` | 7-bag シャッフルによるランダム生成の仕組み |

### 実験

- [ ] `PIECES[0][0]`（I ピース 0°）の値 `0x0F00` を2進数に変換し、4×4グリッドに書き出してみる

```
0x0F00 = 0000 1111 0000 0000
4×4グリッド（ビット15が左上）:
[ ][ ][ ][ ]  ← 上の行（ビット15〜12）
[x][x][x][x]  ← 2行目（ビット11〜8）
[ ][ ][ ][ ]  ← 3行目（ビット7〜4）
[ ][ ][ ][ ]  ← 下の行（ビット3〜0）
```

---

## Step 4：入力制御を読む [ ]

**目安：30分**

読むファイル：`src/input.cpp`

### 重点理解事項

**DAS（Delayed Auto Shift）の仕組み**：
- 初回遅延：16フレーム
- 連射間隔：6フレーム
- `dasTimer` がこの制御を担当

**ボタン判定の使い分け**：
- `arduboy.justPressed()` → 押した瞬間だけ（1回）
- `arduboy.pressed()` → 押し続けている間ずっと

### 確認ポイント

- [ ] DAS の「16フレーム待ってから6フレームごと」になる部分がコードのどこか指摘できる
- [ ] ゲーム状態ごとにどの入力が有効になるか説明できる

---

## Step 5：描画処理を読む [ ]

**目安：45〜60分**

読むファイル：`src/renderer.cpp`

### 画面レイアウト

```
X=0  X=42 X=46                X=128
 |フィールド |  |サイドパネル       |
 | 40×64px  |  | SCORE / LEVEL   |
 |          |  | BEST / NEXT     |
Y=0                             Y=64
```

- 1セル = 4×4px
- フィールド = 10列 × 16行 = 40×64px

### 重点理解事項

| 関数 | 理解するポイント |
|---|---|
| `drawCell()` | `fillRect()` で1ブロック（4×4px）を描画 |
| `drawPiece()` | ピースマスクをビット展開して各セルを描画 |
| `drawGhost()` | `drawRect()`（枠線のみ）で着地予測を薄く表示 |
| `renderFrame()` | ゲーム状態で描画関数を切り替える分岐 |

### 確認ポイント

- [ ] フィールドの左端 X 座標がいくつか確認する（`renderer.cpp` の定数）
- [ ] `drawGhost()` と `drawPiece()` の描画関数の違いを確認する

---

## Step 6：効果音を読む [ ]

**目安：15分**

読むファイル：`src/sound.cpp` + `include/sound.h`

### 重点理解事項

- `const uint16_t SND_LOCK[] PROGMEM` → ArduboyTones の音データ形式
- どのゲームイベントにどの効果音が対応しているか
- EEPROM で音ON/OFFを保存している部分

### 確認ポイント

- [ ] 効果音の種類（何種類あるか）を数える
- [ ] `pgm_read_word()` が必要な理由を説明できる（Flashに格納されているため）

---

## Step 7：小さな改造で動作を確認する [ ]

**目安：任意（改造ごとに30〜60分）**

改造後は必ず `pio run` でビルド確認し、Ardens Player で動作確認すること。

### 改造メニュー（難易度：易→難）

- [ ] **スコア倍率を変える**  
  `game.cpp` の `SCORE_TABLE[]` を編集（例：1行消し 100点 → 200点）

- [ ] **落下速度を変える**  
  `game.cpp` の `FALL_INTERVAL[]` を編集（値を大きくするとゆっくりになる）

- [ ] **ゴーストをOFFにしてみる**  
  `renderer.cpp` の `drawGhost()` 呼び出しをコメントアウト

- [ ] **レベルアップ時に効果音を追加する**  
  `game.cpp` の `clearLines()` 内でレベルアップ時に音再生関数を呼ぶ（`sound.h` の関数を参照）

---

## 最終チェックリスト（全ステップ完了後）

- [ ] `field[3] & (1u << 7)` が表す意味を説明できる（row=3, col=2 にブロックがある）
- [ ] I ピース 0° の `0x0F00` を4×4グリッドに図示できる
- [ ] `pgm_read_word()` が必要な理由を説明できる（RAMではなくFlashに格納されているため）
- [ ] DAS の「16フレーム→6フレームごと」になるコード箇所を指摘できる
- [ ] ゲームオーバーが判定されるタイミングを説明できる（`spawnNext()` でピース出現時に重なった場合）
- [ ] ハイスコアが EEPROM に書き込まれるタイミングを説明できる（ゲームオーバー時）
